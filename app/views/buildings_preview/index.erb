<h1>Přehled budov</h1>


<% @buildings.each do |build| %>

<%#=  build.title %>
<%#= link_to 'zobrazit', buildings_preview_path(build, :id => build.id) %>

    <article class="building">
      <div class="buildingHEader ">
        <%= link_to build.title, buildings_preview_path(build, :id => build.id) %>
      </div>

      <%= link_to image_tag("build1.png", :alt => "building img"), buildings_preview_path(build, :id => build.id) %>
      <br><br>
      <%= link_to 'zobrazit', buildings_preview_path(build, :id => build.id), :class => 'buildingLink' %>
    </article>


<% end %>

<% if @buildings.count == 0 %>
  žádné budovy nenalezeny
<% end %>

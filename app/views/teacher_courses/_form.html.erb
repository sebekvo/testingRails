<%= simple_form_for(@teacher_course) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :teacher_id %>
    <%= f.input :course_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
